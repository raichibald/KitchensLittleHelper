# KitchensLittleHelper

## How to run the example?
1. Clone this repo
2. Open terminal and navigate to project folder
3. Run `pod install`
4. Open `Kitchens Little Helper.xcworkspace` and run the project on selected device or simulator
